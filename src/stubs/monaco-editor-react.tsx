import type { ComponentProps } from 'react';

const MonacoEditor = (_props: ComponentProps<'div'>): JSX.Element => {
  return <div>Monaco editor is unavailable.</div>;
};

export default MonacoEditor;
