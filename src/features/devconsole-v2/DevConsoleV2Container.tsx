import type { PropsWithChildren } from 'react';

export const DevConsoleV2Container = ({ children }: PropsWithChildren): JSX.Element => {
  return <div>{children}</div>;
};

export default DevConsoleV2Container;
